apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./aiostreams
  - ./frigate
  # These three deploy from their own repositories; only the GitRepository and
  # the Flux Kustomization live here.
  - ./lucian-cs
  - ./mushroom-finder
  - ./mytops
  - ./open-webui
  - ./searxng
  - ./spottarr
  - ./stalker-stremio
  - ./uptime-kuma

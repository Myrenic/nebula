apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: subtitles
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: subtitles
  values:
    controllers:
      subtitles:
        containers:
          app:
            image:
              repository: ghcr.io/skoruppa/stremio-community-subtitles
              tag: latest
            env:
              FLASK_ENV: "production"
              FLASK_RUN_PORT: "4949"
              SECRET_KEY: "${SUBTITLES_SECRET_KEY}"
              DISABLE_EMAIL_VERIFICATION: "true"
              PREFERRED_URL_SCHEME: "https"
              TMDB_API_KEY: "${AIOMETADATA_TMDB_API}"
              DATABASE_URL: "mysql+aiomysql://stremio:${SUBTITLES_DB_PASSWORD}@subtitles-db:3306/stremio_subs"
              TZ: "${TZ}"
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 512Mi
            securityContext:
              runAsNonRoot: true
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
    service:
      app:
        ports:
          http:
            port: 4949
    persistence:
      instance:
        # The image writes flask instance files to /app/instance, which is a
        # root-owned image-layer path and not covered by any PVC; give it a
        # writable scratch volume instead of running the container as root.
        type: emptyDir
        globalMounts:
          - path: /app/instance
      uploads:
        # Same for /app/uploads (writable scratch, recreated on restart).
        type: emptyDir
        globalMounts:
          - path: /app/uploads
      data:
        type: persistentVolumeClaim
        storageClass: longhorn
        accessMode: ReadWriteOnce
        size: 5Gi
        globalMounts:
          - path: /app/data
      logs:
        type: persistentVolumeClaim
        storageClass: longhorn
        accessMode: ReadWriteOnce
        size: 2Gi
        globalMounts:
          - path: /app/logs
      subtitles:
        type: persistentVolumeClaim
        storageClass: longhorn
        accessMode: ReadWriteOnce
        size: 20Gi
        globalMounts:
          - path: /app/subtitles

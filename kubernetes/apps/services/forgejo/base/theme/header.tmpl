{{/* Included by templates/base/head.tmpl as the last thing in <head>, i.e. after
     Forgejo's own stylesheet for the selected theme. One line on purpose: the
     page markup is Forgejo's and must stay that way, so this file is the only
     template of ours that every page renders. */}}
<link rel="stylesheet" href="{{AssetUrlPrefix}}/css/nebula.css">

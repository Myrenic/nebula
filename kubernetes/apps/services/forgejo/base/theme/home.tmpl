{{/* The landing page for a visitor who is not signed in. Replaces Forgejo's
     templates/home.tmpl, which is the one page this instance owns outright:
     markup of our own, styled by the nebula- classes in nebula.css. Signed-in
     users are sent to the dashboard by Forgejo itself and never see this.

     A template override is tied to the Forgejo version it was written for - the
     docs say so out loud - so this file is deliberately as small as a page can
     be: head, a hero and four cards, footer. */}}
{{template "base/head" .}}
<div class="nebula-page">
  <section class="nebula-hero">
    <h1>Code, versioned<br>at home</h1>
    <p>Git repositories, pull requests, issues and reviews for everything built here, on three nodes in the house and behind the same account that opens the rest of it.</p>
    <div class="nebula-ctas">
      {{if .IsSigned}}
        <a class="nebula-btn" href="{{AppSubUrl}}/dashboard">Your dashboard</a>
      {{else}}
        <a class="nebula-btn" href="{{AppSubUrl}}/user/oauth2/keycloak">Sign in with Keycloak</a>
      {{end}}
      <a class="nebula-btn nebula-btn-plain" href="{{AppSubUrl}}/explore/repos">Browse repositories</a>
    </div>
  </section>
  <div class="nebula-cards">
    <div class="nebula-card">
      <div class="nebula-icon">
        <svg width="40" height="40" viewBox="0 0 40 40" fill="none" aria-hidden="true">
          <g stroke="currentColor" stroke-width="1.8" stroke-linecap="round">
            <circle cx="13" cy="10" r="4"/>
            <circle cx="13" cy="30" r="4"/>
            <circle cx="28" cy="20" r="4"/>
            <path d="M13 14v12"/>
            <path d="M13 20h11"/>
          </g>
        </svg>
      </div>
      <h3>Git hosting</h3>
      <p>Repositories with pull requests, issues, code review and wikis. Clone over HTTPS with a personal access token.</p>
    </div>
    <div class="nebula-card">
      <div class="nebula-icon">
        <svg width="40" height="40" viewBox="0 0 40 40" fill="none" aria-hidden="true">
          <g stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 7l11 4v9c0 6.6-4.6 11-11 13-6.4-2-11-6.4-11-13v-9l11-4z"/>
            <path d="M15.5 19.5l3.5 3.5 6-6.5"/>
          </g>
        </svg>
      </div>
      <h3>One account</h3>
      <p>Keycloak signs you in: the realm account that opens Grafana and the Traefik dashboard works here too, and your first sign-in creates the account.</p>
    </div>
    <div class="nebula-card">
      <div class="nebula-icon">
        <svg width="40" height="40" viewBox="0 0 40 40" fill="none" aria-hidden="true">
          <g stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <rect x="8" y="9" width="24" height="9" rx="2"/>
            <rect x="8" y="22" width="24" height="9" rx="2"/>
            <path d="M13 13.5h.01"/>
            <path d="M13 26.5h.01"/>
          </g>
        </svg>
      </div>
      <h3>Our own hardware</h3>
      <p>Forgejo 15 and PostgreSQL on three Talos nodes, with the repositories on Longhorn volumes in the cluster that runs everything else here.</p>
    </div>
    <div class="nebula-card">
      <div class="nebula-icon">
        <svg width="40" height="40" viewBox="0 0 40 40" fill="none" aria-hidden="true">
          <g stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M15 13l-6 7 6 7"/>
            <path d="M25 13l6 7-6 7"/>
          </g>
        </svg>
      </div>
      <h3>Open source</h3>
      <p>Forgejo is AGPL-licensed and community-run: no feature gates, no telemetry, and no vendor holding the keys to the repositories.</p>
    </div>
  </div>
</div>
{{template "base/footer" .}}

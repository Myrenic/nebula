# Network-namespace half of the chacdn control RBAC: allows the webui's
# kubectl-proxy sidecar (SA chacdn-control in services) to create/delete the
# per-user IngressRoutes that route to per-user workspace pods. Lives here
# because the chacdn Kustomization forces targetNamespace=services.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: chacdn-control
  namespace: network
rules:
  - apiGroups: ["traefik.io"]
    resources: ["ingressroutes"]
    verbs: ["get", "list", "create", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: chacdn-control
  namespace: network
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: chacdn-control
subjects:
  - kind: ServiceAccount
    name: chacdn-control
    namespace: services
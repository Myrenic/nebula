apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ks.yaml
# No namespace.yaml and no base/ here: the cdi Namespace is shipped (and patched) by
# the bundle that cdi-operator applies, and base/ is that Kustomization's path.

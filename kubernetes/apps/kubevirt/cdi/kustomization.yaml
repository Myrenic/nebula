apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ks.yaml
  # The Namespace is listed here rather than by the kubevirt-level kustomization
  # so that this directory stays the single entry point for everything CDI-related.
  - ./namespace.yaml

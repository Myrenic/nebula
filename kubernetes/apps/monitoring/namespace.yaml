apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
  labels:
    # privileged on purpose: node-exporter uses hostNetwork and hostPath mounts (/proc, /sys, /var/lib).
    pod-security.kubernetes.io/enforce: privileged
